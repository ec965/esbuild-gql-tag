{
  "name": "esbuild-gql-tag",
  "version": "1.0.0",
  "description": "An esbuild plugin to parse `.graphql` files into an AST at compile time.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.8",
    "graphql": "^16.6.0"
  },
  "peerDependencies": {
    "graphql": "^16"
  },
  "dependencies": {
    "graphql-tag": "^2.12.6"
  }
}
